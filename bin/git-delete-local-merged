#!/bin/sh
#
# Delete all local and remote branches that have been merged into HEAD.

git branch -r --merged | grep -v "$(git rev-parse --abbrev-ref HEAD)" | sed 's/origin\///' | xargs -n 1 git push --delete origin